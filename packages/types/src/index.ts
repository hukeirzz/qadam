// Shared TypeScript types for Kadam, consumed by both apps/mobile and
// apps/school-web. Consolidated from what previously lived, duplicated, in
// apps/mobile/src/lib/supabase.ts's Database type and each service file's
// local interfaces — populated in a follow-up step.
export {};
