# @qadam/config

Reserved for shared ESLint/Prettier config once one is introduced (neither
exists in this repo yet). The shared TypeScript base config lives at the
repo root (`tsconfig.base.json`) so every workspace package/app can extend
it with a relative path regardless of nesting depth.
